version: 2.1
orbs:
  node: circleci/node@12.7
jobs:
  build-and-test:
    executor:
      name: node/default
    steps:
      - checkout
      - run:
          name: Install Serverless CLI and dependencies
          command: |
            sudo npm i -g serverless
workflows:
  build-and-test:
    jobs:
      - build-and-test
